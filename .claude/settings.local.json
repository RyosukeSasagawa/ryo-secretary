{
  "permissions": {
    "allow": [
      "Bash(git init)",
      "Bash(git add CLAUDE.md requirements.txt .gitignore && git status)",
      "Bash(cd ~/projects/ryo-secretary && pip install -r requirements.txt 2>&1)",
      "Bash(cd ~/projects/ryo-secretary && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt 2>&1)"
    ]
  }
}
